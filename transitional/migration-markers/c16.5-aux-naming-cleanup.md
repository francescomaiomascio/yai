# C16.5 — Tests, Tools and Transitional Naming Cleanup

Status: completed
Date: 2026-03-11

## Scope
- Renamed noisy task/milestone-shaped scripts in `tests/integration/workspace/`.
- Renamed LAN qualification scripts from `ql_*`/`run_qw*` to concise scenario-first names.
- Renamed source-plane integration scripts to canonical non-versioned names.
- Renamed verbose tool files in `tools/validate/` and `tools/gen/`.
- Reduced verbosity for selected `transitional/` marker/map filenames.

## Guardrail
- Added `tools/validate/validate_aux_naming.py`.
- Integrated into:
  - `tools/release/unified_repo_convergence_smoke.sh`
  - `tools/bin/yai-governance-compat-check`
  - `tests/unit/governance/run_governance_unit_tests.sh`

## Notes
- References were updated across scripts, Makefile, docs, and transitional maps.
- The guardrail blocks reintroduction of task-coded naming patterns in auxiliary areas.

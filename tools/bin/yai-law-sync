#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
echo "DEPRECATION: yai-law-sync now delegates to yai-law-embed-sync" >&2
exec "$ROOT/tools/bin/yai-law-embed-sync" "$@"

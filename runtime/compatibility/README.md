# Runtime Compatibility (Transitional)

This path is transitional compatibility only.

`runtime/` is treated as migration source, not as the future system center.
New canonical ownership is rooted in `kernel/`, `sys/`, and `user/`.

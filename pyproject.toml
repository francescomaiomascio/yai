[project]
name = "ice-runtime"
version = "0.1.0"
description = "ICE Runtime – cognitive system runtime primitives (lifecycle, logging, ids, time, typing)"
requires-python = ">=3.10"


dependencies = [
  "orjson>=3.9.0",
  "typing-extensions>=4.8.0"
]
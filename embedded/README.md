# Embedded (Legacy Transitional Surface)

`embedded/` is a transitional legacy surface.

Rules:

- do not treat `embedded/` as canonical repository topology
- do not introduce new canonical governance content here
- use `transitional/embedded-law/` for migration markers
- move target content to `governance/` during convergence

Final-state target excludes `embedded/`.

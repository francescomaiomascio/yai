# Governance Public Headers (Canonical)

`include/yai/governance/` is the canonical public governance namespace of the
unified YAI system.

Transitional note:

- legacy compatibility headers remain under `include/yai/law/`
- new public includes should prefer `yai/governance/*`

Current A3 stance:

- path-level cutover (`law` -> `governance`) is active
- type-level rename is deferred to dedicated migration slices

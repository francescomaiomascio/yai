# Edge Public Headers

Public subordinate edge runtime surface (daemon identity/state, binding,
mediation, action points, delegated scope handling).

Migration note:

- legacy edge headers under `include/yai/daemon/` remain transitional
